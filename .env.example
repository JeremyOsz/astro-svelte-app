# Ephemeris backend
EPHEMERIS_URL=https://immanuel-astro.onrender.com
EPHEMERIS_API_KEY=your-ephemeris-api-key

# OpenAI chat backend
OPENAI_API_KEY=your-openai-api-key

# Supabase + Drizzle
PUBLIC_SUPABASE_URL=
PUBLIC_SUPABASE_ANON_KEY=
DATABASE_URL=

# Optional: require Authorization: Bearer <token> on /api/chat
CHAT_API_BEARER_TOKEN=

# Optional: require an 8-digit passcode on /api/chat
CHAT_ACCESS_PASSCODE=

# Google Search Console — HTML tag method only: paste the "content" value from
# Search Console → Settings → Ownership verification (not a reCAPTCHA key).
PUBLIC_GOOGLE_SITE_VERIFICATION=
