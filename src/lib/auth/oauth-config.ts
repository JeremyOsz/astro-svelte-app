/** Flip to true when Google OAuth is enabled and configured in Supabase. */
export const GOOGLE_OAUTH_AVAILABLE = false;

/** When true, email/password sign-in is disabled in the UI. Set false when Supabase auth works again. */
export const LOGIN_TEMPORARILY_UNAVAILABLE = true;
